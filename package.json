{
  "name": "chat_app",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm i --prefix server && npm run dev --prefix server\" \"npm i --prefix client && npm run dev --prefix client\"",
    "start": "concurrently \"npm i --prefix server && start --prefix server\" \"npm i --prefix client &&npm start --prefix client\""
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.17.0"
  }
}